
• evaluer output de la derniere entree
    ((1 > 2)) ou ls porn
    => code de retour 0 : si c'est vrai
    => code de retour 1 : si c'est faux
        dans ce cas retournera 1 que ce soit pour ((1 > 2)) et ls porn
        - fichier porn n'existe pas sur la machine
        - 1 > 2 est faux

• Symbole de comparaison
    -lt -le -gt -ge -eq -ne
     [le] : less or equal
     [ne] : not equal

• Prendre en compte les strings avec espace ou appliquer les regex
    if [[ -f $file ]] 
    if [[ $answer =~ ^y(es)?$ ]]
        - fait que $file peut contenir des espaces "Mon Dossier Personnel"
•

•



